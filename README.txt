/*
	Classy was designed using Netbeans derby database, and is not guaranteed to run properly 
	when compiled with other IDEs.
	
	When creating your own derby database it should be named "Classy" and have the username
	and password both be "root"
*/